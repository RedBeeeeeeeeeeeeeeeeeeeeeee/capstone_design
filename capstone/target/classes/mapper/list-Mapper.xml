<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.list-Mapper">

	<select id="getListOne" resultType="lists">
		SELECT * FROM furnlist
		<where>
			<if test="type != 'Type'">
				type = #{type}
			</if>
			<if test="brand != 'Brand'">
				AND brand = #{brand}
			</if>
			<if test="price != 'Price'">
				AND price = #{price}
			</if>
		</where>
	</select>
	
	<select id="getBasketList" resultType="lists">
		SELECT	b.num, f.furnName, f.type, f.brand, f.price, f.imgLink, f.detailLink 
		FROM basket b
		JOIN furnlist f
		ON b.num = f.num
		WHERE b.ID = #{ID}	
	</select>

</mapper>